/* 
 * Generated by ASDT 
*/ 

class com.adgamewonderland.agw.net.RemotingBeanCaster {
	
	public static function getCastedInstance(instance:Object, values:Object ):Object
	{
		// schleife ueber alle variablen im result
		for (var index:String in values) {
			// weiter wenn "serviceName"
			if (index == "serviceName") {
				continue;	
			}
			// aktueller wert
			var value = values[index];
			// index mit grossbuchstaben beginnen
			index = index.charAt(0).toUpperCase() + index.substr(1);
			// function
			var func:Function = instance["set" + index];
//			trace(index + " # " + value + " # " + func.toString());
			// wert uebergeben
			func.apply(instance, [value]);
		}
		// zurueck geben
		return instance;
	}
}
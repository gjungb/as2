/*klasse:			ResultUIautor: 			gerd jungbluth, adgamewonderlandemail:			gerd.jungbluth@adgamewonderland.dekunde:			skandiaerstellung: 		08.12.2004zuletzt bearbeitet:	01.02.2005durch			gjstatus:			in bearbeitung*/import com.adgamewonderland.skandia.akademietool.quiz.*;class com.adgamewonderland.skandia.akademietool.quiz.ResultUI extends MovieClip {	// Attributes		private var myResult:Result;		private var difficulty_txt:TextField, tasks_txt:TextField, correct_txt:TextField, wrong_txt:TextField, rate_txt:TextField;		private var back_mc:MovieClip;		// Operations		// konstruktor	public function ResultUI( )	{		// dargestelltes ergebnis		myResult = null;		// schwierigkeitsgrad rechtsbuendig		difficulty_txt.autoSize = "right";		// anzahl aufgaben zentriert		tasks_txt.autoSize = "center";		// anzahl korrekt zentriert		correct_txt.autoSize = "center";		// anzahl nicht korrekt zentriert		wrong_txt.autoSize = "center";		// quote rechtsbuendig		rate_txt.autoSize = "center";	}		public function showResult(robj:Result, hex:Number, format:TextFormat ):Void	{		// dargestelltes ergebnis		myResult = robj;		// hintergrund einfaerben		// var col:Color = new Color(back_mc);		// col.setRGB(hex);		// schwierigkeitsgrad 		difficulty_txt.text = myResult.difficulty;		// abbrechen, wenn keine aufgaben		if (myResult.numtaskstotal == 0) return;		// anzahl aufgaben 		tasks_txt.text = String(myResult.numtaskstotal);		tasks_txt.setTextFormat(format);		// anzahl korrekt 		correct_txt.text = String(myResult.numtaskscorrect);		correct_txt.setTextFormat(format);		// anzahl nicht korrekt 		wrong_txt.text = String(myResult.numtaskswrong);		wrong_txt.setTextFormat(format);		// quote 		rate_txt.text = String(Math.round(myResult.rate * 100)) + " %";		rate_txt.setTextFormat(format);	}	} /* end class ResultUI */
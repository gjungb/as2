/*klasse:			Summaryautor: 			gerd jungbluth, adgame-wonderlandemail:			gerd.jungbluth@adgame-wonderland.dekunde:			skandiaerstellung: 		08.12.2004zuletzt bearbeitet:	31.01.2005durch			gjstatus:			in bearbeitung*/import com.adgamewonderland.skandia.akademietool.quiz.*;class com.adgamewonderland.skandia.akademietool.quiz.Summary {	// Attributes		private var myResults:Array;		// Operations		// konstruktor	public function Summary()	{		// ergebnis der berechnung		myResults = [];	}		public function get results():Array	{		// ergebnis der berechnung		return (myResults);	}		public function calculateResults(difficulties:Array, tasks:Array ):Void	{		// bisherige ergebnisse loeschen		clearResults();		// fuer jede schwierigkeitsgrad ein ergebnis		for (var i:Number = -1; ++i < difficulties.length; ) {			// neues ergebnis			myResults.push(new Result(difficulties[i]));		}		// alle aufgaben auswerten		for (var j:Number = -1; ++j < tasks.length; ) {			// aktuelle aufgabe			var task:Task = tasks[j];			// schwierigkeitsgrad			var difficulty:Number = task.difficulty.num;			// ergebnis dieses schwierigkeitsgrads			var result:Result = myResults[difficulty - 1];			// anzahl der aufgaben hochzaehlen			result.numtaskstotal ++;			// anzahl korrekt / nicht korrekt geloester aufgaben hochzaehlen			if (task.getScore() > 0) {				// korrekt				result.numtaskscorrect ++;			} else {				// nicht korrekt				result.numtaskswrong ++;			}		}		// gesamtergebnis		var total:Result = new Result("Gesamtauswertung");		// zusammenzaehlen		for (var k:String in myResults) {			// aktuelles ergebnis			result = myResults[k];			// anzahl der aufgaben			total.numtaskstotal += result.numtaskstotal;			// korrekt			total.numtaskscorrect += result.numtaskscorrect;			// nicht korrekt			total.numtaskswrong += result.numtaskswrong;		}		// zu den ergebnissen zufuegen		myResults.push(total);	}		private function clearResults():Void	{		// alle ergebnisse loeschen		for (var i:String in myResults) delete(myResults[i]);		// array leeren		myResults.splice();	}	} /* end class Summary */